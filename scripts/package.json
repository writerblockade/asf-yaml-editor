{
  "name": "asf-yaml-verify",
  "version": "1.0.0",
  "private": true,
  "description": "Headless verification for the .asf.yaml Validator & Builder. Core needs only Node builtins; the dev-deps below enable the optional jsdom + ajv checks.",
  "scripts": {
    "verify": "node verify.js"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "jsdom": "^24.0.0"
  }
}
